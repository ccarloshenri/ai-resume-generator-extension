export enum AIProviderName {
  OpenAI = "openai",
  Gemini = "gemini",
  Claude = "claude",
}

export enum Language {
  English = "en",
  Portuguese = "pt",
}

export enum Page {
  Settings = "settings",
  Profile = "profile",
  Analyze = "analyze",
  Result = "result",
}

export enum MessageType {
  ScanJob = "SCAN_JOB",
  JobScanResult = "JOB_SCAN_RESULT",
  ScanError = "SCAN_ERROR",
}
